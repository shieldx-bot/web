apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: disk-io-saturation
  namespace: backend
spec:
  mode: all
  selector:
    namespaces:
      - backend
    labelSelectors:
      chaos-target: "true"
  stressors:
    # Compatible with older Chaos Mesh versions that do not support stressngStressors.
    cpu:
      workers: 2
      load: 80
    memory:
      workers: 1
      size: "1GB"
  duration: "30m"
---
apiVersion: batch/v1
kind: Job
metadata:
  name: disk-fill-85-percent
  namespace: backend
spec:
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app: disk-fill
    spec:
      restartPolicy: Never
      nodeSelector:
        chaos-target: "true"
      containers:
        - name: fill
          image: busybox:1.36
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              mkdir -p /data/fill
              # Fill quickly, hold for metrics, then release to create recovery samples.
              dd if=/dev/zero of=/data/fill/blob.bin bs=1M count=20480
              sleep 1800
              rm -f /data/fill/blob.bin
              sync
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir:
            sizeLimit: 25Gi
